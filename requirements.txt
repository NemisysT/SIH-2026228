# Runtime dependencies. Install into a virtual environment:
#   python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
#
# Installation needs a package index; RUNTIME does not touch the network.
numpy>=2.0,<3
scipy>=1.13
scikit-learn>=1.5
pillow>=10.3
pydantic>=2.7
typer>=0.12
rich>=13.7
cryptography>=42
pyyaml>=6

# Development / test
pytest>=8
pytest-cov>=5

# Module 2 model runtimes. OPTIONAL: without them the corresponding model
# formats are unavailable and the pipeline reports NOT_ASSESSED with a reason,
# rather than failing or silently passing. Nothing here is fetched at run time.
onnx>=1.16
onnxruntime>=1.18
torch>=2.2
